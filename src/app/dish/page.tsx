import React from 'react';

import CustomModal from '@/components/custom-modal/custom-modal';

const Page = () => {
  return <CustomModal modalTitle={'Add New Item'} isDishModal={true} />;
};

export default Page;
